% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state_space.R
\name{fit_random_walk_drift}
\alias{fit_random_walk_drift}
\title{Fit the random-walk + drift model (TDLLHPP)}
\usage{
fit_random_walk_drift(
  y_ts,
  sample_start,
  mprior_level,
  vprior_level,
  param_lhpp,
  param_name = "input"
)
}
\arguments{
\item{y_ts}{A bimets ts of \code{log(LA)} (or \code{log(LPOP)}).}

\item{sample_start}{\code{"YYYYQq"} string; the first quarter in EViews's
estimation \code{smpl}.}

\item{mprior_level}{Scalar prior mean for the initial level.}

\item{param_lhpp}{Variance ratio σ²_obs / σ²_state.}

\item{param_name}{For error messages only.}
}
\value{
List of \verb{(TLEVEL = bimets_ts, TDRIFT = bimets_ts)}.
}
\description{
KFAS port of supply_side.prg lines 78-108. The drift \code{c} is a single
parameter (EViews \code{C(1)}); the legacy code surfaces it as a constant
series via \code{tdllhpp = ss_lhpp.@coefs(1)}. We reproduce that — \code{TDRIFT}
is constant across all quarters, equal to the data-driven smoothed
drift estimate. Within EViews's estimation smpl \code{TLLHPP} is the
smoothed level; beyond it, the EViews script carries the last value
forward, which we also do.
}
\keyword{internal}

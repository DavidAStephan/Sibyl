% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dummies.R
\name{apply_dummies}
\alias{apply_dummies}
\title{Apply deterministic dummy / trend series}
\usage{
apply_dummies(database, catalogue = series_catalogue())
}
\arguments{
\item{database}{Named list of bimets TIMESERIES.}

\item{catalogue}{\code{\link[=series_catalogue]{series_catalogue()}} (unused in v0; reserved for
migrating the spec into the catalogue).}
}
\value{
The database with dummy series added.
}
\description{
Reads \code{inst/extdata/dummies.csv} and materialises each row as a bimets
TIMESERIES over the database's existing span (or \link{DEFAULT_DUMMY_START}
to \link{DEFAULT_DUMMY_END} if the database is empty). Idempotent: rows
already in the database are left alone.
}
\keyword{internal}

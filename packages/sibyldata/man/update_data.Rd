% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_data.R
\name{update_data}
\alias{update_data}
\title{Pull the public-data panels SIBYL needs}
\usage{
update_data(
  vintage = Sys.Date(),
  sources = "all",
  refresh = FALSE,
  tolerate_failures = TRUE
)
}
\arguments{
\item{vintage}{A \code{Date} identifying the data vintage. Stamped into every
row's \code{vintage} column and used as the cache key. Defaults to today.}

\item{sources}{Character vector of sources to refresh. Default:
\code{"all"} — every implemented source. Pass an explicit vector to
restrict (e.g. \code{c("fred")} for FRED-only).}

\item{refresh}{Logical. If \code{FALSE} (default), cached vintages are
returned untouched. \code{TRUE} re-pulls.}

\item{tolerate_failures}{Logical. If \code{TRUE} (default), a single
source's failure (typically a transient network error) emits a
warning and the run continues with the surviving sources — the
downstream pipeline can fall back to the fixture via
\code{\link[=merge_with_fallback]{merge_with_fallback()}}. If \code{FALSE}, any source failure halts the run.}
}
\value{
A tidy tibble of \verb{(series_id, source, date, value, vintage)}.
}
\description{
Top-level orchestrator. Routes each requested source through
\code{\link[=fetch_source]{fetch_source()}} (which handles caching and source-specific dispatch) and
concatenates results into one long tidy tibble.
}
\details{
All source fetchers cache their output as parquet under \code{\link[=cache_path]{cache_path()}} —
a re-run on the same vintage returns instantly. \code{refresh = TRUE} re-pulls.

v0 status: \code{fred} is implemented end-to-end against the live API;
\code{abs}, \code{rba}, \code{oecd}, \code{worldbank}, \code{bom} are stubs that throw informative
errors when called. The catalogue
(\code{\link[=series_catalogue]{series_catalogue()}}) covers all sources so the institutional knowledge
is preserved even ahead of the fetchers.
}

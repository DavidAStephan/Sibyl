% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state_space.R
\name{apply_state_space_trends}
\alias{apply_state_space_trends}
\title{Apply the supply-side state-space trend handlers}
\usage{
apply_state_space_trends(database, catalogue = series_catalogue())
}
\arguments{
\item{database}{Named list of bimets TIMESERIES.}

\item{catalogue}{\code{\link[=series_catalogue]{series_catalogue()}} (unused in v0).}
}
\value{
The database with trend series added.
}
\description{
For each variable in \link{SUPPLY_PARAM} whose input is available in the
database, runs the corresponding KFAS state-space estimator and
inserts the smoothed trend(s) back into the database. Idempotent:
rows already in the database are left alone.
}
\details{
Currently materialises \code{TDLLA}, \code{TDLLPOP}, \code{TDLLHPP} from \code{LA}, \code{LPOP},
\code{LHPP}. Also produces the smoothed log-level trends (\code{TLLA}, \code{TLLPOP},
\code{TLLHPP}) as a side effect. Skips a row if the required input is
missing.
}
\keyword{internal}

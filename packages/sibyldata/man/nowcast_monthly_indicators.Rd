% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monthly_indicators.R
\name{nowcast_monthly_indicators}
\alias{nowcast_monthly_indicators}
\title{Return monthly bimets ts for catalogue series with frequency "M"}
\usage{
nowcast_monthly_indicators(raw, vars = NULL, catalogue = series_catalogue())
}
\arguments{
\item{raw}{A tidy panel from \code{\link[=update_data]{update_data()}} with columns
\verb{(series_id, source, date, value, vintage)}.}

\item{vars}{Optional character vector of \code{martin_var} codes to
restrict the result to (e.g. \code{c("LE", "HOURS", "RT")}). When
\code{NULL} (the default) returns all monthly catalogue series the
panel covers.}

\item{catalogue}{Catalogue tibble; defaults to \code{\link[=series_catalogue]{series_catalogue()}}.}
}
\value{
Named list of \code{bimets::TIMESERIES} keyed by \code{martin_var}.
Empty list if no monthly rows are in scope.
}
\description{
Filters the raw panel down to catalogue rows tagged
\code{source_frequency = "M"}, joins to the catalogue to pick up the
\code{martin_var} name, then pivots each series to a monthly \code{bimets}
time series. Returns a named list keyed by \code{martin_var}.
}
\details{
This is the parallel of \code{to_martin_database()} for monthly data.
It does \strong{not} apply Chow-Lin / splices / derived formulas —
those are quarterly-database concerns. Each returned series is the
raw monthly observation panel.
}

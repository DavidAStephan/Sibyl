% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identities.R
\name{apply_iad_weights}
\alias{apply_iad_weights}
\title{Apply the IAD weights from vendored io_calcs.prg output}
\usage{
apply_iad_weights(database, catalogue = series_catalogue())
}
\arguments{
\item{database}{Named list of bimets ts.}

\item{catalogue}{\code{\link[=series_catalogue]{series_catalogue()}} (unused in v0).}
}
\value{
Database with IAD_W_C, IAD_W_GC, IAD_W_GI, IAD_W_I, IAD_W_X
added.
}
\description{
Reads \code{inst/extdata/iad_weights.csv} (a verbatim copy of
\code{references/MARTIN-master/Data/t_iad.csv} — the output of EViews's
\code{io_calcs.prg}, which interpolates ABS input-output table omega
values across 13 years) and inserts each of the five IAD weight
series into the database.
}
\details{
Vendoring the CSV avoids reimplementing the IO-table extraction (13
year-specific XLS files with year-specific cell ranges) — a session
unto itself with limited additional fidelity since the omega values
only update when ABS releases new IO tables. A separate follow-up
could add a \code{update_iad_weights()} that reads fresh IO tables.
}
\keyword{internal}

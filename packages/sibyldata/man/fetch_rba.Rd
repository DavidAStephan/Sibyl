% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_rba.R
\name{fetch_rba}
\alias{fetch_rba}
\title{Fetch RBA series and return a tidy panel}
\usage{
fetch_rba(series_ids, vintage = Sys.Date())
}
\arguments{
\item{series_ids}{Character vector of catalogue source IDs.}

\item{vintage}{A \code{Date}. Stamped into every row's \code{vintage} column.}
}
\value{
A tibble of \verb{(series_id, source, date, value, vintage)}.
}
\description{
Wraps \code{readrba::read_rba(series_id = ...)} over a vector of catalogue
source IDs. The catalogue uses Quandl-style prefixed IDs (e.g.
\code{"F02_1_FCMYGBAG2"}, \code{"D02_DLCACS"}, \code{"G01_GCPIOCPMTMQP"}) so that the
RBA table prefix is preserved as institutional metadata; the readrba
package takes only the bare series ID after the last underscore. This
function strips the prefix at fetch time and keeps the original catalogue
ID in the returned panel's \code{series_id} column so the
\code{\link[=to_martin_database]{to_martin_database()}} join still resolves.
}
\details{
Series without an underscore are passed through unchanged (these are the
bare IDs the catalogue uses for newer RBA series like \code{FXRUSD},
\code{FXRTWI}, \code{FLRBFOLBT}).
}

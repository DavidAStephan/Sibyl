% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tpw.R
\name{build_tpw_cpi}
\alias{build_tpw_cpi}
\title{Build a trading-partner-weighted CPI index via OECD}
\usage{
build_tpw_cpi(
  weights = tpw_partner_weights(),
  observation_start = "2000-01-01",
  base_quarter = "2010Q1"
)
}
\arguments{
\item{weights}{Optional override for \code{\link[=tpw_partner_weights]{tpw_partner_weights()}}.}

\item{observation_start}{Date passed through to \code{\link[=fetch_oecd]{fetch_oecd()}}.}

\item{base_quarter}{Base for the index. Default "2010Q1".}
}
\value{
A \code{bimets::TIMESERIES} quarterly index ~100 at base_quarter,
or \code{NULL} if no partner data could be fetched.
}
\description{
Fetches monthly CPI growth-rate-over-1-year for each partner from
OECD's prices dataflow, aggregates monthly to quarterly, weighted by
\code{\link[=tpw_partner_weights]{tpw_partner_weights()}}. Returns a quarterly bimets ts suitable as a
WP proxy.
}
\details{
\strong{Status:} working v0 for WP only. GDP (WY) and export-price (WPX)
equivalents need their OECD QNA / trade dataflow keys figured out
(the QNA key shape is a 13-position vector that varies by which
measure is selected — see next_session.md item 2).
}

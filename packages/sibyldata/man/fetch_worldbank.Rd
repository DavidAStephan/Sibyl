% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_worldbank.R
\name{fetch_worldbank}
\alias{fetch_worldbank}
\title{Fetch World Bank commodity price indices from the bundled xlsx}
\usage{
fetch_worldbank(
  source_rows,
  vintage = Sys.Date(),
  xlsx_path = worldbank_xlsx_path()
)
}
\arguments{
\item{source_rows}{A tibble subset of \code{\link[=series_catalogue]{series_catalogue()}} for
\code{worldbank}.}

\item{vintage}{A \code{Date}.}

\item{xlsx_path}{Path to the xlsx. Defaults to the bundled fixture.}
}
\value{
A tibble of \verb{(series_id, source, date, value, vintage)}.
}
\description{
Reads the "Monthly Indices" sheet of the World Bank Pink Sheet (CMO)
xlsx bundled at
\code{references/MARTIN-master/Data/CMO-Historical-Data-Monthly.xlsx}. Returns
just the columns named in the requested \code{source_rows$source_id} vector
(e.g. \code{iAGRICULTURE} for WPAG, \code{iENERGY} for WPCOM).
}
\details{
Dates in the source xlsx are \code{YYYYMnn} strings (e.g. \verb{1960M01}); we parse
to first-of-month \code{Date}s so quarterly aggregation in
\code{\link[=to_martin_database]{to_martin_database()}} works correctly.

The bundled xlsx is a frozen historical snapshot. A future v0.x will
fetch the live World Bank file from
\code{pubdocs.worldbank.org/en/.../CMO-Historical-Data-Monthly.xlsx}; for now
the static file keeps the fetcher deterministic.
}

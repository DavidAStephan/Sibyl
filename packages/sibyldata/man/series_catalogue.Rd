% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{series_catalogue}
\alias{series_catalogue}
\title{The series catalogue}
\usage{
series_catalogue()
}
\value{
A tibble of the catalogue.
}
\description{
The institutional knowledge for SIBYL: each row maps a MARTIN variable to
a source series ID plus the metadata needed to fetch and shape it. Lifted
and extended from
\code{references/MARTIN-master/Programs/import_data.prg}.
}
\details{
Columns:
\itemize{
\item \code{martin_var} — the MARTIN variable name (uppercase for model endogenous
variables; lowercase for raw imports that get transformed downstream).
\item \code{source} — one of \code{abs}, \code{rba}, \code{fred}, \code{oecd}, \code{worldbank}, \code{bom},
\code{derived}.
\item \code{source_id} — the source's series identifier (\code{A2304081W} for ABS,
\code{GDPC1} for FRED, etc.). \code{NA} for derived series.
\item \code{source_table} — catalogue / table reference where it helps (e.g.
\code{"5206.0"} for ABS, \code{"F02"} for RBA). \code{NA} otherwise.
\item \code{source_frequency} — \code{M}, \code{Q}, \code{A}, or \code{D} for monthly / quarterly /
annual / daily.
\item \code{aggregation} — how to convert to quarterly (\code{mean}, \code{sum}, \code{last},
\code{first}). \code{NA} when source is already quarterly.
\item \code{transformation} — \code{direct} (just rename), \code{spliced} (needs
backcasting / splicing), \code{chowlin} (Chow-Lin annual→quarterly),
\code{level_from_pct} (cumulate from a base), \code{derived} (computed from other
catalogue entries), \code{dummy} (deterministic calendar dummy / trend,
spec in \code{inst/extdata/dummies.csv}), \code{scalar} (constant series,
spec in \code{inst/extdata/scalars.csv}).
\item \code{description} — plain English (consumed by the LLM and humans alike).
\item \code{units} — free text.
}
}

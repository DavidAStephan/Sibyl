% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_abs.R
\name{fetch_abs}
\alias{fetch_abs}
\title{Fetch ABS series and return a tidy panel}
\usage{
fetch_abs(source_rows, vintage = Sys.Date())
}
\arguments{
\item{source_rows}{A tibble subset of \code{\link[=series_catalogue]{series_catalogue()}} for \code{abs}.}

\item{vintage}{A \code{Date}.}
}
\value{
A tibble of \verb{(series_id, source, date, value, vintage)}.
}
\description{
Pulls every ABS catalogue (cat_no) listed in \code{source_rows}, then filters
to the requested series IDs. \code{readabs::read_abs()} accepts a \code{cat_no}
plus a tables argument; for v0 we pull every table of each catalogue
since per-table targeting isn't captured in the catalogue yet
(institutional knowledge for which-table-by-series lives in the legacy
\href{../../../references/MARTIN-master/Programs/import_data.prg}{import_data.prg}
and could be lifted as a v0.1 column if download time becomes an issue).
}
\details{
All downloads are cached at the source-and-vintage level via the
caller (\code{fetch_source()}), so a re-run within the same vintage is a
no-op.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{database_provenance}
\alias{database_provenance}
\title{Read a database's provenance table}
\usage{
database_provenance(db)
}
\arguments{
\item{db}{A MARTIN database (named list of bimets \code{TIMESERIES}) produced by
\code{\link[=to_martin_database]{to_martin_database()}} / \code{\link[=merge_with_fallback]{merge_with_fallback()}}.}
}
\value{
A tibble \verb{(variable, source_class)}, or \code{NULL} when no provenance
attribute is present (e.g. a hand-built database). \code{source_class} is one
of live, fixture_fallback, vendored_wf1, proxy, dummy, derived, unknown.
}
\description{
Accessor for the \code{"provenance"} attribute attached by
\code{\link[=to_martin_database]{to_martin_database()}} (and refined by \code{\link[=merge_with_fallback]{merge_with_fallback()}}). The table
records, for each MARTIN variable in the database, where its values actually
came from -- so a report never has to guess whether a series was a live
fetch, a fixture backfill, a vendored \code{.wf1} splice, or a proxy.
}

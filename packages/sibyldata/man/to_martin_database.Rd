% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_data.R
\name{to_martin_database}
\alias{to_martin_database}
\title{Pivot a raw tidy panel to MARTIN's variable names}
\usage{
to_martin_database(raw, frequency = "Q")
}
\arguments{
\item{raw}{A tibble from \code{\link[=update_data]{update_data()}}.}

\item{frequency}{Target frequency. Only \code{"Q"} is supported in v0.}
}
\value{
A named list of \code{bimets::TIMESERIES}, with attributes:
\itemize{
\item \code{skipped}: list of skipped martin_vars grouped by reason.
\item \code{derived_added}: character vector of derived vars that materialised.
}
}
\description{
Joins the catalogue (\code{source_id} → \code{martin_var}), then routes each row
through the appropriate transformation handler. Returns a named list of
\code{bimets::TIMESERIES} keyed by MARTIN variable name, ready for
\code{\link[martin:load_martin]{martin::load_martin()}}.
}
\details{
The pipeline (in order):
\enumerate{
\item \strong{Direct / level_from_pct / spliced rows} → standard quarterly pivot
via \code{\link[=pivot_quarterly]{pivot_quarterly()}} (handles monthly→quarterly aggregation per
each row's \code{aggregation} rule).
\item \strong{Chow-Lin rows} → annual pivot, then disaggregated to quarterly
via \code{\link[=apply_chowlin]{apply_chowlin()}} using \code{tempdisagg::td()}.
\item \strong{\code{apply_level_from_pct()}} → replaces PTM, P with cumulated level
indices from registered bases (1982Q1).
\item \strong{\code{apply_splices()}} → forward-extends or backward-fills target
series from spliced sources (NCR_HIST → NCR; NBR_SPLICE → NBR).
\item \strong{\code{apply_pim()}} → integrates V into KV via the perpetual-inventory
method from a 1980Q1 base.
\item \strong{\code{add_derived_series()}} → evaluates catalogue formula expressions
(PC, PG, PM, TOT, NHA, NHNW, HCOE, etc.) in a fixed-point loop.
}

Anything that can't be materialised is reported in the \code{"skipped"}
attribute, grouped by reason.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_bom.R
\name{fetch_bom}
\alias{fetch_bom}
\title{Fetch the Southern Oscillation Index from BoM}
\usage{
fetch_bom(
  series_ids,
  vintage = Sys.Date(),
  url = "ftp://ftp.bom.gov.au/anon/home/ncc/www/sco/soi/soiplaintext.html"
)
}
\arguments{
\item{series_ids}{Character vector. The only valid value is \code{"SOI"};
passed for API uniformity with the other fetchers.}

\item{vintage}{A \code{Date}.}

\item{url}{Source URL. Defaults to BoM's published plaintext table.}
}
\value{
A tibble of \verb{(series_id, source, date, value, vintage)}.
}
\description{
Pulls the plaintext SOI table from BoM's ftp (HTML-wrapped). Parses the
fixed-format \verb{YEAR JAN FEB ... DEC} rows into a tidy panel.
}
\details{
One series only, \code{SOI}. The catalogue's \code{source_id} is \code{SOI} too.

Network-dependent: skips test calls fall through to the caller if BoM
is unreachable.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extend_exogenous.R
\name{extend_exogenous}
\alias{extend_exogenous}
\title{Extend variables in a database forward to a target quarter}
\usage{
extend_exogenous(database, end_quarter, rules = "carry_all")
}
\arguments{
\item{database}{Named list of bimets/base TIMESERIES.}

\item{end_quarter}{Character \code{"yyyyQq"} — the inclusive end of the
extension range.}

\item{rules}{Named list keyed by martin_var. Each entry is a list with
at least \code{mode} ∈ \{\code{"carry"}, \code{"constant"}, \code{"linear"}\}; mode
\code{"constant"} also takes \code{value}; mode \code{"linear"} takes optional
\code{lookback} (default 4 quarters). If \code{rules = "carry_all"}, every
series in \code{database} is carried forward.}
}
\value{
The database with extended series.
}
\description{
For each named entry in \code{rules}, extends the matching series in
\code{database} so it covers up to \code{end_quarter}. Quarters that already have
data are not overwritten. Series not in \code{rules} are left untouched —
the caller chooses which variables get extended.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_oecd.R
\name{fetch_oecd}
\alias{fetch_oecd}
\title{Fetch OECD series and return a tidy panel}
\usage{
fetch_oecd(
  series_ids,
  vintage = Sys.Date(),
  observation_start = "1959-01-01",
  base_url = "https://sdmx.oecd.org/public/rest/data",
  timeout_s = 60
)
}
\arguments{
\item{series_ids}{Character vector of OECD SDMX keys (see above).}

\item{vintage}{A \code{Date}. Stamped into every row's \code{vintage} column.}

\item{observation_start}{Date string passed through as the
\code{startPeriod} parameter. Default \code{"1959-01-01"} matches MARTIN's
horizon start.}

\item{base_url}{SDMX endpoint root. Override for testing.}

\item{timeout_s}{Per-series timeout (seconds). Default 60.}
}
\value{
A tibble of \verb{(series_id, source, date, value, vintage)}.
Empty when \code{series_ids} has length 0.
}
\description{
Hits the OECD SDMX REST API (\verb{https://sdmx.oecd.org/public/rest/data/...})
and parses the returned CSV. Each catalogue row's \code{source_id} is
expected to be a fully-qualified SDMX key of the form
\code{"<dataflow>/<key>"}, e.g. \code{"OECD.SDD.NAD,DSD_NAMAIN1@DF_QNA,1.0/AUS.S1..B1GQ.LR..L.Q?"}.
The function passes the segment after the slash as the SDMX key and
the segment before as the dataflow identifier.
}
\details{
The OECD API does not require an API key for public data; default
rate limits are around 20 requests per minute per IP. Cached per
vintage via the usual sibyldata parquet store so repeated runs are
fast.

\strong{Status (v0):} this is a working \emph{single-series} fetcher. The
trading-partner-weighted world-aggregate computation that MARTIN's
WY / WP / WPX expect is \strong{not} implemented yet -- the catalogue
continues to use FRED US proxies for those. A proper TPW build would
need:
\enumerate{
\item Fetch OECD quarterly GDP / CPI / export-price series for each
of Australia's top trading partners (CN, JP, US, KR, SG, IN,
NZ, GB, ...).
\item Source partner export-share weights (ABS table 5368.0).
\item Combine: WY = sum_i weight_i * partner_i_GDP_index.
That's a separate workstream; this function is the first piece.
}
}

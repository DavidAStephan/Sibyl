% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{classify_provenance}
\alias{classify_provenance}
\title{Classify catalogue variables into provenance source classes}
\usage{
classify_provenance(variables, catalogue = series_catalogue())
}
\arguments{
\item{variables}{Character vector of MARTIN variable names.}

\item{catalogue}{The catalogue tibble. Defaults to \code{\link[=series_catalogue]{series_catalogue()}}.}
}
\value{
A tibble \verb{(variable, source_class)}, one row per input variable,
in input order. \code{source_class} is one of
live, fixture_fallback, vendored_wf1, proxy, dummy, derived, unknown.
}
\description{
Maps each MARTIN variable named in \code{variables} to its intended
\link[=database_provenance]{provenance} \code{source_class}, derived purely from the
catalogue's \code{transformation} / \code{source} / \code{description} columns. This is the
\emph{intended} class as built by \code{\link[=to_martin_database]{to_martin_database()}} from a freshly fetched
panel; \code{\link[=merge_with_fallback]{merge_with_fallback()}} later promotes individual variables to
\code{"fixture_fallback"} when the live path supplied no value.
}
\details{
Mapping rules (first match wins):
\itemize{
\item \code{transformation} in {\code{dummy}, \code{scalar}} -> \code{"dummy"}.
\item \code{transformation} in {\code{derived}, \code{identity}} -> \code{"derived"}.
\item \code{transformation} == \code{state_space} -> \code{"vendored_wf1"}.
\item a real data row whose \code{description} mentions "proxy" -> \code{"proxy"}.
\item any other real data row (direct / spliced / chowlin / level_from_pct
from abs / rba / fred / oecd / worldbank / bom) -> \code{"live"}.
\item a variable not present in the catalogue -> \code{"unknown"}.
}
}

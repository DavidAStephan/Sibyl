% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state_space.R
\name{fit_local_linear_trend}
\alias{fit_local_linear_trend}
\title{Fit the local-linear-trend model (TDLLA / TDLLPOP)}
\usage{
fit_local_linear_trend(
  y_ts,
  sample_start,
  mprior,
  vprior,
  param_trend,
  param_drift,
  param_name = "input"
)
}
\arguments{
\item{y_ts}{A bimets ts of \code{log(LA)} (or \code{log(LPOP)}).}

\item{sample_start}{\code{"YYYYQq"} string; the first quarter in EViews's
estimation \code{smpl}.}

\item{mprior}{2-vector of initial (level, drift) prior means. Only
the level is treated as informative; the drift uses a diffuse
prior so its smoothed value is data-driven.}

\item{param_trend, param_drift}{Variance ratios from supply_side.prg.}

\item{param_name}{For error messages only.}
}
\value{
List of \verb{(TLEVEL = bimets_ts, TDRIFT = bimets_ts)}.
}
\description{
KFAS port of supply_side.prg lines 17-44 (TDLLA) and 46-73 (TDLLPOP).
Returns smoothed \code{TLEVEL} (log-trend level) and \code{TDRIFT} (log-trend
growth) as bimets TIMESERIES over the full input span — the drift
state is carried forward past the EViews estimation sample end via
the random-walk dynamics, matching the legacy "extend to last
observation" behaviour.
}
\keyword{internal}

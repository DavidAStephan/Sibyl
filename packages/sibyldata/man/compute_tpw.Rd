% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tpw.R
\name{compute_tpw}
\alias{compute_tpw}
\title{Compute a trading-partner-weighted aggregate index}
\usage{
compute_tpw(partner_series, weights, base_quarter = "2010Q1")
}
\arguments{
\item{partner_series}{Named list of \code{bimets::TIMESERIES} objects,
one per partner. Names must match the names in \code{weights}.}

\item{weights}{Named numeric vector summing to ~1 (see
\code{\link[=tpw_partner_weights]{tpw_partner_weights()}}).}

\item{base_quarter}{\code{"yyyyQq"} string identifying the base period for
normalisation. Defaults to \code{"2010Q1"}.}
}
\value{
A \code{bimets::TIMESERIES} of the weighted-average index. NA
where no partners contribute.
}
\description{
Given per-partner time series and a partner-weight vector, builds a
weighted-average index normalised to 100 at \code{base_quarter}.
}
\details{
Each partner series is independently normalised so that its value at
\code{base_quarter} equals 100; the aggregate is then
\verb{sum_i weight_i * partner_i_index_t}. Partners with no observation
at \code{base_quarter} are dropped from the aggregate at that period
(their weight reallocated proportionally across remaining partners).
}

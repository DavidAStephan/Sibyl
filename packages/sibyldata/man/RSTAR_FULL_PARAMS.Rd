% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state_space.R
\docType{data}
\name{RSTAR_FULL_PARAMS}
\alias{RSTAR_FULL_PARAMS}
\title{Fit the faithful 11-state RSTAR Okun-Phillips state-space}
\format{
An object of class \code{list} of length 6.
}
\usage{
RSTAR_FULL_PARAMS
}
\arguments{
\item{database}{Named list of bimets ts (needs Y, PTM, LUR, NCR, PI_E).}

\item{sample_start}{\code{"YYYYQq"} string. Default \code{"1986Q3"} matches
rstar.prg's smpl.}
}
\value{
List with \code{RSTAR}, \code{YGAP}, \code{YPOT}, \code{G}, \code{Z} (bimets ts).
}
\description{
Full KFAS port of \code{rstar.prg} (with state-augmented intercept handling).
The EViews state vector is 11-dimensional:
}
\details{
alpha = (ygap, ygapL1, ygapL2, ygapL3, ypot, g, NAIRU, NAIRUL1,
nrate, nrateL1, z)

We add a 12th constant \code{ONE} state so the time-varying intercept on
the ygap equation (an exogenous function of \code{rcash} lags) can be
encoded via a time-varying T matrix entry T\link{1, 12, t}. The signal
equations are:

lrgdp_t = ypot_t + ygap_t                            (deterministic identity)
LUR_t   = NAIRU_t + beta_1 * (0.4 * ygap_t + 0.3 * ygapL1_t +
0.2 * ygapL2_t + 0.1 * ygapL3_t) + e2
dlptm_t = (1 - gamma_1) * pi_eq_t +
gamma_1/3 * (dlptm_{t-1} + dlptm_{t-2} + dlptm_{t-3}) +
gamma_2 * (LUR_{t-1} - NAIRUL1_t) + e3

Structural parameters (alpha_1..3, beta_1, gamma_1..2) are
pre-estimated via OLS using HP-like smoothed proxies as initial
state values; KFAS then estimates the five state-innovation variances
(sigma_1, sigma_4..7) via \code{fitSSM}. The dlptm signal's lagged-inflation
AR(3) and pi_eq terms are pre-subtracted from the observation to give
a modified observation y3_mod_t = -gamma_2 * NAIRUL1_t + e3 that
KFAS handles linearly.

Returns RSTAR (the smoothed \code{nrate} state) plus the auxiliary states
(\code{YGAP}, \code{YPOT}, \code{G}, \code{Z}) as bimets ts for diagnostics.
}
\keyword{internal}

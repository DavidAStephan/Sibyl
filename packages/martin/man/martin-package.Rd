% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/martin-package.R
\docType{package}
\name{martin-package}
\alias{martin}
\alias{martin-package}
\title{martin: SIBYL wrapper around the bimets implementation of MARTIN}
\description{
The Reserve Bank of Australia's MARTIN macroeconometric model, solved via
the \code{bimets} package. Built on the code in
\verb{references/bimets-main/}; the model definition files are vendored into
\verb{inst/extdata/} of this package with attribution.
}
\details{
The public surface is small:
\itemize{
\item \code{\link[=load_martin]{load_martin()}} — read the bimets model file and a database, return a
loaded bimets model object.
\item \code{\link[=solve_martin]{solve_martin()}} — solve baseline plus add-factor scenarios; returns a
tidy projection tibble.
\item \code{\link[=equation_catalogue]{equation_catalogue()}} — the equation menu the LLM sees, with
plain-English descriptions, sector groupings, and which equations are
eligible for adjustment.
\item \code{\link[=martin_data_fixture]{martin_data_fixture()}} — the frozen MARTINDATA_XLSX snapshot used by
regression tests.
}

v0 uses the \code{MARTINMOD_AF.txt} form. That file holds 95 \verb{BEHAVIORAL>}
equations; only ~51 carry a \code{RESTRICT> c1=1}, and the rest impose real
cross-coefficient restrictions (e.g. \code{c4+c5+c6+c7=1}, \code{c4=0.5}).
\code{bimets::ESTIMATE()} re-fits the free coefficients on EVERY load — it is
not loading the published EViews values as-is. The default ("frozen")
only means estimating over the model file's embedded 2019Q3 \code{TSRANGE}
sample; the alternative is re-estimating through a later quarter, which
re-fits across the COVID break (see \code{\link[=solve_martin]{solve_martin()}}'s \code{coefficients}).
}
\author{
\strong{Maintainer}: David Stephan \email{david.stephan@gmail.com}

}
\keyword{internal}

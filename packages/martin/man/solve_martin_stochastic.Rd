% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve_martin.R
\name{solve_martin_stochastic}
\alias{solve_martin_stochastic}
\title{Solve MARTIN with stochastic uncertainty bands}
\usage{
solve_martin_stochastic(
  database,
  adjustments = NULL,
  horizon,
  coefficients = c("frozen", "reestimated"),
  estimation_end = NULL,
  scenario = "baseline",
  n_draws = 200L,
  band_start = NULL,
  ...
)
}
\arguments{
\item{database}{A named list of \code{bimets::TIMESERIES}, eventually from
\code{\link[sibyldata:to_martin_database]{sibyldata::to_martin_database()}}; in tests, from \code{\link[=read_fixture]{read_fixture()}}.}

\item{adjustments}{A \code{judgement::adjustment_list} (possibly empty or NULL).}

\item{horizon}{A length-2 character vector of \code{c("yyyyQq", "yyyyQq")}
identifying the inclusive simulation range.}

\item{coefficients}{Which estimation sample to use. Both settings run
\code{bimets::ESTIMATE()}, which re-fits each behavioural equation's free
coefficients (the AF form is genuinely behavioural — see
\code{\link[=load_martin]{load_martin()}}); they differ only in the sample end date.
\code{"frozen"} (default) estimates over the model file's embedded \code{TSRANGE}
end of 2019Q3, reproducing the originally-published in-sample fit.
\code{"reestimated"} re-fits every behavioural equation over its embedded
start through \code{estimation_end}. Use it deliberately: extending the
sample past 2019Q3 re-fits the coefficients ACROSS the COVID break,
which materially changes them and departs from the published model —
the project default is to leave coefficients frozen unless asked.}

\item{estimation_end}{Optional \code{"yyyyQq"} string. Required when
\code{coefficients = "reestimated"}. Ignored under \code{"frozen"}.}

\item{scenario}{A label written into the returned tibble.}

\item{n_draws}{Integer number of stochastic replicas. Default 200.}

\item{...}{Passed through to \code{\link[=solve_martin]{solve_martin()}} in the fallback path
(e.g. \code{exogenize}, \code{baseline_for_exogenize}).}
}
\value{
A tidy tibble with columns
\verb{(variable, quarter, value, lower, upper, scenario)}. \code{value} is the
central (deterministic) path; \code{lower}/\code{upper} are the 2.5\%/97.5\%
band edges. Attribute \code{n_draws} records the replica count; attribute
\code{band_method} is \code{"stochsimulate"} or \code{"af_perturbation"}.
}
\description{
An opt-in companion to \code{\link[=solve_martin]{solve_martin()}} that propagates coefficient /
equation-error uncertainty into the projection via a Monte Carlo
simulation, returning a central path plus a lower/upper band per
(variable, quarter). The deterministic \code{\link[=solve_martin]{solve_martin()}} is unchanged and
remains the default path; callers opt into bands explicitly by calling
this function.
}
\details{
Mechanism. When \code{bimets::STOCHSIMULATE()} is available (it is in the
vendored bimets >= 4.x), each behavioural equation's disturbance is
perturbed across \code{n_draws} replicas with mean zero and the equation's own
regression standard error
(\verb{behaviorals$<EQ>$statistics$StandardErrorRegression}), then the model is
re-solved per replica. Bands are the empirical 2.5\%/97.5\% quantiles of the
replica matrix (\code{model$simulation_MM}); \code{value} is the deterministic
(first-column) solution, identical to \code{\link[=solve_martin]{solve_martin()}}'s central path.

Fallback. If \code{STOCHSIMULATE} is not exported by the installed bimets, we
fall back to a documented coefficient-perturbation scheme: we draw
\code{n_draws} deterministic solves, each re-running \code{\link[=solve_martin]{solve_martin()}} after
jittering every behavioural add-factor by a normal shock scaled to that
equation's residual standard deviation. This is coarser (it perturbs the
add-factor rather than the regression coefficients directly) but yields a
comparable spread without STOCHSIMULATE.
}

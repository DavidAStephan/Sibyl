% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_features.R
\name{model_features}
\alias{model_features}
\title{Optional MARTIN model features (baseline-neutral by default)}
\description{
The enhancements from \code{docs/martin_enhancements_plan.md} are delivered as
opt-in \emph{features}: load-time transforms of the bimets model text plus the
data seeding bimets requires (every endogenous variable must have a series
present in the database). With no features requested the model text is
returned verbatim, so the frozen, no-adjustment baseline stays bit-identical
to the bimets reference (design principle 6).
}
\details{
Implemented features:
\describe{
\item{\code{output_gap}}{CES production function inverted EMMA-style. Adds the
identities \code{KSTAR}, \code{NSTAR}, \code{YSTAR}, \code{YGAP}, \code{LESTAR}. Requires the
labour-augmenting efficiency trend \code{EFF} in the database (see
\code{\link[sibyldata:fit_efficiency_trend]{sibyldata::fit_efficiency_trend()}}). Calibration via
\verb{feature_params$ces_*}.}
\item{\code{external_accounting}}{Net-foreign-liability / current-account stock
accounting. Adds \code{NTB}, \code{TB_GDP}, \code{NCA}, \code{VNFL}, \code{NFL_GDP}, \code{CAD_GDP}.
Uses \code{NFOY}/\code{NTRF} if present (else 0) and a \code{VNFL} seed.}
\item{\code{fiscal_accounting}}{Government budget-and-debt accounting. Adds
\code{NREV}, \code{NSPEND}, \code{NLEND}, \code{INTG}, \code{BG}, \code{BG_GDP}, \code{DEF_GDP}.
\code{feature_params$fiscal_mode = "demo"} (default) uses proxy inputs with a
balanced auto-calibration; \code{"reconciled"} (I-2) matches the balance to the
realised ABS income account (\code{NG} + income-account payables = total
outlays, corporate tax on \code{GOS}), needing the real fiscal series +
\code{income_side}. Debt is in annual-GDP units. See \code{docs/income_side_scope.md}.}
\item{\code{household_income}}{(I-2 household) Baseline-neutral decomposition of
the lumped household non-labour income \code{NHOY} into income-account parts.
Adds \code{HH_NONLAB} (non-labour primary income), \code{NHOY_RESID}, \code{NHDY_RECON}
and \code{HH_TAXRATE}, using real ABS household-account series \code{HH_PRIMARY}
(A2302917X) and \code{NHTAX} (A2302937J) when present. Leaves \code{NHOY}/\code{NHDY}/\code{RC}
untouched. See \code{docs/income_side_scope.md}.}
\item{\code{income_side}}{(I-0) Income side of GDP: the decomposition
\code{NY = NHCOE + GOS + GMI + TAX_PROD_NET}. Adds \code{GOS} (gross operating
surplus, as the residual), \code{PROFIT_SHARE} and \code{LABOUR_SHARE}. \code{GMI} and
\code{TAX_PROD_NET} are real ABS series (5206.0) when present, else share-of-GDP
proxies. See \code{docs/income_side_scope.md}.}
\item{\code{corporate_accelerator}}{(I-3, T3) Balance-sheet financial accelerator.
Adds the corporate-leverage identity \code{LEV} (debt-to-annual-GDP, from ABS
\code{DCORP} A3427913W or a proxy) and re-estimates the business spread \code{NBRSP}
with a lagged-leverage term, so a more-indebted corporate sector widens the
external-finance premium (review F1/F5/SF4). Off by default.}
\item{\code{endogenous_household}}{(I-2 Phase 2, T3) Replaces the \code{NHOY} ECM with
an identity rebuilt from its components, with household income tax
responding to primary income (\code{NHTAX_EN}) and transfers to unemployment
(\code{NTRANSFERS_EN}) -- automatic stabilisers that flow into consumption \code{RC}.
Needs \code{household_income}. Opt-in (re-baselines \code{RC}).}
\item{\code{fx_premium}}{(T2) Debt-elastic exchange-rate risk premium: adds a
\code{NFL_GDP} term to the \code{RTWI} equation. Needs \code{external_accounting}.}
\item{\code{fiscal_rule}}{(T2) Debt-stabilising fiscal rule on the effective
household tax rate. Needs \code{fiscal_accounting}.}
\item{\code{convex_ptm}}{(T3) Convex Phillips curve: swaps the linear \code{c7*LURGAP}
gap term in \code{PTM} for the reciprocal \code{c7*(LURGAP/LUR)} form (re-estimated).}
\item{\code{inverted_le}}{(T3) Capital-aware employment: swaps the reduced-form
error-correction target in \code{LE} for the inverted-production-function
employment \code{LESTAR} (re-estimated). Needs \code{output_gap}.}
\item{\code{elb_floor}}{(T1, fidelity) Restores the EViews effective-lower-bound
floor on the cash rate (\code{equations.prg} L538, dropped by the bimets port).
Renames the Taylor-rule behavioural to \code{NCR_RULE} (preserving estimation +
residual handover) and makes \code{NCR} a floored identity at
\code{feature_params$elb_floor_value} (default 0.1). Baseline-neutral: the floor
never binds in sample so the solve is unchanged; it only bites in a
forecast that eases below the bound. NB: with this on, cash-rate add-factors
must target \code{NCR_RULE}, since \code{NCR} is now an identity.}
}
}

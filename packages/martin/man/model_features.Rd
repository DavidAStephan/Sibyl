% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_features.R
\name{model_features}
\alias{model_features}
\title{Optional MARTIN model features (baseline-neutral by default)}
\description{
The enhancements from \code{docs/martin_enhancements_plan.md} are delivered as
opt-in \emph{features}: load-time transforms of the bimets model text plus the
data seeding bimets requires (every endogenous variable must have a series
present in the database). With no features requested the model text is
returned verbatim, so the frozen, no-adjustment baseline stays bit-identical
to the bimets reference (design principle 6).
}
\details{
Implemented features:
\describe{
\item{\code{output_gap}}{CES production function inverted EMMA-style. Adds the
identities \code{KSTAR}, \code{NSTAR}, \code{YSTAR}, \code{YGAP}, \code{LESTAR}. Requires the
labour-augmenting efficiency trend \code{EFF} in the database (see
\code{\link[sibyldata:fit_efficiency_trend]{sibyldata::fit_efficiency_trend()}}). Calibration via
\verb{feature_params$ces_*}.}
\item{\code{external_accounting}}{Net-foreign-liability / current-account stock
accounting. Adds \code{NTB}, \code{TB_GDP}, \code{NCA}, \code{VNFL}, \code{NFL_GDP}, \code{CAD_GDP}.
Uses \code{NFOY}/\code{NTRF} if present (else 0) and a \code{VNFL} seed.}
\item{\code{fiscal_accounting}}{Government budget-and-debt accounting. Adds
\code{NREV}, \code{NSPEND}, \code{NLEND}, \code{INTG}, \code{BG}, \code{BG_GDP}, \code{DEF_GDP}. Uses
effective tax rates / transfers as exogenous inputs.}
\item{\code{fx_premium}}{(T2) Debt-elastic exchange-rate risk premium: adds a
\code{NFL_GDP} term to the \code{RTWI} equation. Needs \code{external_accounting}.}
\item{\code{fiscal_rule}}{(T2) Debt-stabilising fiscal rule on the effective
household tax rate. Needs \code{fiscal_accounting}.}
\item{\code{convex_ptm}}{(T3) Convex Phillips curve: swaps the linear \code{c7*LURGAP}
gap term in \code{PTM} for the reciprocal \code{c7*(LURGAP/LUR)} form (re-estimated).}
\item{\code{inverted_le}}{(T3) Capital-aware employment: swaps the reduced-form
error-correction target in \code{LE} for the inverted-production-function
employment \code{LESTAR} (re-estimated). Needs \code{output_gap}.}
}
}

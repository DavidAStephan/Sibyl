% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve_martin.R
\name{solve_martin}
\alias{solve_martin}
\title{Solve MARTIN over a horizon, optionally with add-factors}
\usage{
solve_martin(
  database,
  adjustments = NULL,
  horizon,
  coefficients = c("frozen", "reestimated"),
  estimation_end = NULL,
  scenario = "baseline",
  exogenize = character(0),
  baseline_for_exogenize = NULL,
  exogenize_range = NULL,
  features = character(0),
  feature_params = list(),
  sim_convergence = 1e-06,
  sim_iter_limit = 100
)
}
\arguments{
\item{database}{A named list of \code{bimets::TIMESERIES}, eventually from
\code{\link[sibyldata:to_martin_database]{sibyldata::to_martin_database()}}; in tests, from \code{\link[=read_fixture]{read_fixture()}}.}

\item{adjustments}{A \code{judgement::adjustment_list} (possibly empty or NULL).}

\item{horizon}{A length-2 character vector of \code{c("yyyyQq", "yyyyQq")}
identifying the inclusive simulation range.}

\item{coefficients}{Which estimation sample to use. Both settings run
\code{bimets::ESTIMATE()}, which re-fits each behavioural equation's free
coefficients (the AF form is genuinely behavioural — see
\code{\link[=load_martin]{load_martin()}}); they differ only in the sample end date.
\code{"frozen"} (default) estimates over the model file's embedded \code{TSRANGE}
end of 2019Q3, reproducing the originally-published in-sample fit.
\code{"reestimated"} re-fits every behavioural equation over its embedded
start through \code{estimation_end}. Use it deliberately: extending the
sample past 2019Q3 re-fits the coefficients ACROSS the COVID break,
which materially changes them and departs from the published model —
the project default is to leave coefficients frozen unless asked.}

\item{estimation_end}{Optional \code{"yyyyQq"} string. Required when
\code{coefficients = "reestimated"}. Ignored under \code{"frozen"}.}

\item{scenario}{A label written into the returned tibble.}

\item{sim_convergence}{Bimets simulation convergence tolerance.}

\item{sim_iter_limit}{Bimets simulation iteration limit.}
}
\value{
A long tidy tibble of \verb{(variable, quarter, value, scenario)},
with attributes \code{horizon}, \code{adjustments}, and \code{scenario}.
}
\description{
The public surface of \code{martin}. Takes a starting database, a list of
add-factors (from \code{judgement::adjustment_list()}), and a horizon; returns
a long tidy projection tibble.
}
\details{
Pipeline:
\enumerate{
\item \code{\link[=load_martin]{load_martin()}} the chosen variant against \code{database}. ESTIMATE
populates the per-equation residual slots used in step 2.
\item \strong{Replay history}: build a baseline \code{ConstantAdjustment} list from
\verb{model$behaviorals$<EQ>$residuals} so that with no user-provided
adjustment, the simulated path reproduces history to within solver
tolerance (typically ~1e-3 on levels, not bit-exact — the Gauss-Seidel
solver converges to \code{sim_convergence}, and identities re-aggregate the
fitted components). This is the bimets equivalent of EViews
\code{addinit(v=n)} from \code{references/MARTIN-master/Programs/solve_model.prg}.
\item \strong{Inject user adjustments}: expand \code{adjustments} to numeric vectors
via \code{\link[judgement:expand_adjustments]{judgement::expand_adjustments()}} and inject them into the replay
AFs via bimets \verb{[[year, quarter]]<-} per-cell assignment — the same
pattern \code{references/bimets-main/BIMETS_MARTIN_LOAD.R} uses to deliver
a shock. User values are added on top of the residual; equations not
in \code{adjustments} keep their unmodified replay AF.
\item Call \code{bimets::SIMULATE(model, TSRANGE = ..., ConstantAdjustment = ...)}.
\item Pivot the named-list-of-ts result to a long tibble.
}

Horizons that extend past the data end are supported: the replay AFs
(residuals) are decayed forward via the EViews \verb{_a = _a(-1) * -0.5}
convention so they remain well-defined into the future. Note that
bimets still needs every variable's data to extend over \code{TSRANGE};
caller must extend exogenous variables (PI_TARGET, dummies, etc.)
separately when solving past the historical data end.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_matrix.R
\name{sensitivity_matrix}
\alias{sensitivity_matrix}
\title{Pre-compute sensitivity of headline aggregates to per-equation unit shocks}
\usage{
sensitivity_matrix(
  database,
  baseline,
  horizon,
  estimation_end = NULL,
  shock_start = NULL,
  shock_quarters = 4L,
  measure_offsets = c(1L, 4L, 8L, 16L),
  targets = c("Y", "RC", "GNE", "LUR", "PTM", "P", "NCR"),
  equations = NULL,
  probe_curvature = TRUE,
  progress = interactive()
)
}
\arguments{
\item{database}{The same database passed to \code{\link[=solve_martin]{solve_martin()}}.}

\item{baseline}{A baseline projection tibble (from \code{\link[=solve_martin]{solve_martin()}} with
no adjustments) used as the comparison point. Must cover the offsets
measured.}

\item{horizon}{Length-2 \code{c("yyyyQq", "yyyyQq")}.}

\item{estimation_end}{Optional \code{"yyyyQq"}. When non-NULL, behaviorals are
re-estimated through this quarter before the shocks (same convention as
\code{\link[=solve_martin]{solve_martin()}}). NULL = frozen model coefficients.}

\item{shock_start}{\code{"yyyyQq"} for the first quarter of the sustained
shock. Default \code{NULL} derives it dynamically as
\code{horizon[2] - max(measure_offsets)} so the longest offset always
lands inside the horizon. Override to a specific quarter if you want
the propagation measured against a particular regime.}

\item{shock_quarters}{Integer length of the sustained shock. Default 4.}

\item{measure_offsets}{Integer vector of quarter offsets from \code{shock_start}
at which to record deviations. Default \code{c(1, 4, 8, 16)}.}

\item{targets}{Character vector of MARTIN variables whose deviations are
recorded. Default = headline aggregates.}

\item{equations}{Optional character vector restricting the shocks to a
subset of equations. Default NULL = all adjustable equations from
\code{\link[=equation_catalogue]{equation_catalogue()}}.}

\item{probe_curvature}{Logical. If \code{TRUE} (default), also solve each
equation at 3x the standardized shock and emit the \code{deviation_3x},
\code{curvature_ratio}, and \code{linearity_ok} columns so the caller can judge
where the linear-scaling assumption holds. Roughly doubles the cost.}

\item{progress}{Logical; if TRUE prints a one-line progress update per
equation. Default \code{interactive()}.}
}
\value{
A tibble with one row per (equation, target, offset). Columns:
\code{equation}, \code{units}, \code{typical_af_sd}, \code{shock_value}, \code{shock_quarters},
\code{target}, \code{offset_q}, \code{deviation}, \code{deviation_pct}, \code{converged}
(logical: did the 1x solve leave finite target values?), and — when
\code{probe_curvature = TRUE} — \code{deviation_3x}, \code{curvature_ratio}
(\code{deviation_3x / (3 * deviation)}, ~1 if linear) and \code{linearity_ok}
(\code{abs(curvature_ratio - 1) < 0.25}). \code{deviation}/\code{deviation_3x} are NA
when the corresponding solve does not converge; the curvature columns
are NA when \code{probe_curvature = FALSE}.
Attributes: \code{shock_start}, \code{measure_offsets}, \code{baseline_scenario}.
}
\description{
For each adjustable behavioural equation, applies a standardized add-factor
shock of size \verb{1 x typical_af_sd} sustained over \code{shock_quarters} with a
\code{decay_50} tail, solves the model, and records the deviation from baseline
on each of \code{targets} at offsets \code{measure_offsets} quarters after the shock
start. The resulting matrix is intended for inclusion in the
\code{judgement::propose_adjustments()} system prompt so the LLM can reason
about realised propagation instead of guessing.
}
\details{
The model is loaded and ESTIMATEd once and re-used across all shocks, so
the cost is approximately N_equations x simulate_time (a few seconds per
equation) rather than N x full_solve.

\strong{Linearity is not assumed.} MARTIN is nonlinear, so a deviation measured
at one shock size cannot be scaled freely to size a 12-20-quarter shock.
With \code{probe_curvature = TRUE} (the default) each equation is also solved at
3x the standardized shock; the extra columns \code{deviation_3x},
\code{curvature_ratio} (= \code{deviation_3x / (3 * deviation)}, ~1 if linear) and
\code{linearity_ok} (= \code{abs(curvature_ratio - 1) < 0.25}) let the caller see
where linear scaling is safe and where it is not. These are NA when the
probe is disabled or the 3x solve does not converge.

\strong{Garbage is never handed to the prompt.} Each shock's convergence is
captured; if a solve leaves NaN/Inf in the simulated targets, that row's
\code{deviation} (and \code{deviation_3x}) are set to NA and \code{converged = FALSE},
rather than silently emitting a meaningless number.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_martin.R
\name{model_file_path}
\alias{model_file_path}
\title{Path to one of the vendored MARTIN model files}
\usage{
model_file_path(variant = c("af", "identity", "est"))
}
\arguments{
\item{variant}{One of \code{"af"} (default — \code{MARTINMOD_AF.txt}, the behavioural
form with a \code{ConstantAdjustment} add-factor slot on every equation),
\code{"identity"} (\code{MARTINMOD.txt}, pure-identity equivalent), or \code{"est"}
(\code{MARTINMOD_EST.txt}, the form intended for re-estimation). Note that
\code{"af"} is genuinely behavioural: of its 95 \verb{BEHAVIORAL>} equations only
~51 carry a \code{RESTRICT> c1=1}; the rest impose real cross-coefficient
restrictions (e.g. \code{c4+c5+c6+c7=1}, \code{c4=0.5}). \code{bimets::ESTIMATE()}
re-fits the free coefficients on every load — it does not replay
published EViews values as-is.}
}
\value{
Absolute path to the \code{.txt} file.
}
\description{
The bimets \code{.txt} model definitions are vendored into \verb{inst/extdata/} from
\verb{references/bimets-main/}. This helper returns the absolute path on disk.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_martin.R
\name{load_martin}
\alias{load_martin}
\title{Load a MARTIN bimets model with data}
\usage{
load_martin(
  database,
  variant = c("af", "identity", "est"),
  estimate = TRUE,
  estimation_end = NULL,
  features = character(0),
  feature_params = list()
)
}
\arguments{
\item{database}{A named list of \code{bimets::TIMESERIES} keyed by MARTIN
variable name. Eventually produced by
\code{\link[sibyldata:to_martin_database]{sibyldata::to_martin_database()}}; for now, by \code{\link[=read_fixture]{read_fixture()}} in tests.}

\item{variant}{Which model file to load. See \code{\link[=model_file_path]{model_file_path()}}.}

\item{estimate}{Logical. If \code{TRUE} (default), call \code{bimets::ESTIMATE()}
after loading data. Skip only for path-checking; the residual slots
downstream code uses are populated by ESTIMATE.}

\item{estimation_end}{Optional \code{"yyyyQq"} string. When set, rewrites
every \code{TSRANGE} line's end date to this quarter before bimets
loads the model — letting ESTIMATE re-fit the behavioural
coefficients on data through the supplied end (typically the
latest National Accounts release). Per-equation start dates are
preserved. The model file's frozen default is 2019Q3.}
}
\value{
A loaded bimets model object.
}
\description{
Wraps the canonical pattern from
\code{references/bimets-main/BIMETS_MARTIN_LOAD.R}:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{MARTIN <- bimets::LOAD_MODEL("MARTINMOD_AF.txt")
MARTIN <- bimets::LOAD_MODEL_DATA(MARTIN, data)
MARTIN <- bimets::ESTIMATE(MARTIN)
}\if{html}{\out{</div>}}

For the default \code{variant = "af"}, ESTIMATE actually re-fits the free
coefficients of all 95 behavioural equations on every load — it does NOT
merely confirm imposed values. Only ~51 of those equations carry a
\code{RESTRICT> c1=1}; the rest impose genuine cross-coefficient restrictions
(e.g. \code{c4+c5+c6+c7=1}). ESTIMATE also computes each equation's residuals,
and those residuals are what the add-factor pipeline consumes downstream.
"Frozen" coefficients (the default) means estimating over the model file's
embedded 2019Q3 \code{TSRANGE} sample, not loading published EViews values.
}
